# Astro
dist/
.astro/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Env
.env
.env.production
.env.local

# macOS
.DS_Store

# Editor
.idea/
.vscode/

# Audit captures (raw HTML refs, keep tracked? — yes, keep for now)
# _audit/  ← intentionally tracked

# Pre-encode backups (originals of compressed media, local-only)
_originals_backup/

# Local QA capture/diff artifacts (regenerable)
_capture/
