# artificenyc.org

**Production deploy** is built from the monorepo at **`../studioartifice.com/`**:

- Static export / Framer mirror: `public-org/`
- Worker + routing: `deploy/artificenyc/`
- Deploy scripts: `../studioartifice.com/scripts/deploy-artificenyc-framer.sh`
- CI: `../studioartifice.com/.github/workflows/deploy-cloudflare-pages.yml`

This folder is for **domain-specific notes**, DNS checklists, or future split if the nonprofit site gets its own repository.
